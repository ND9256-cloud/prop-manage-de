// src/lib/csv-import.ts
// CSV import is operator-only.
// Use scripts/csv-import.ts instead.
// See scripts/README.md for usage.
export function importCSVTransactions(): never {
    throw new Error(
        'CSV import is operator-only. Use scripts/csv-import.ts'
    );
}
